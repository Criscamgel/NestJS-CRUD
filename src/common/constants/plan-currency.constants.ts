/** Moneda por defecto de planes y cobros Bold (multimoneda USD). */
export const DEFAULT_PLAN_CURRENCY = 'USD';

/** Mínimo Bold para links en COP (pesos enteros). */
export const BOLD_MIN_TOTAL_AMOUNT_COP = 1000;

/** Mínimo Bold para links en USD (dólares enteros). */
export const BOLD_MIN_TOTAL_AMOUNT_USD = 1;
